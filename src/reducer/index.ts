import {NOTES_ACTION_TYPE} from "./textEditorReducer";
import {NoteTypes} from "./textEditorReducer";
import {initialState} from "./textEditorReducer";
import {textEditorReducer} from "./textEditorReducer";

export {
    textEditorReducer,
    NOTES_ACTION_TYPE,
    initialState
};

export type {NoteTypes};